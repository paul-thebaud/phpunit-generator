    /**
     * @var {{ class.getName }} $instance The class instance to test
     */
    protected $instance;

    /**
     * Build the instance of {{ class.getName }}
     */
    protected function setUp()
    {
{% if class.hasMethod('__construct') %}
        $this->instance = new {{ class.getName }}({{ class.getMethod('__construct').generateValues | raw }});
{% else %}
        $this->instance = new {{ class.getName }}();
{% endif %}
    }