    /**
     * The trait/abstract class instance to test
     * @var {{ class.getName }} $instance
     */
    protected $instance;

    /**
     * Build the instance of {{ class.getName }}
     */
    protected function setUp()
    {
{% if class.hasMethod('__construct') %}
        $this->instance = $this->getMockBuilder({{ class.getName }}::class)
            ->setConstructorArgs([{{ class.getMethod('__construct').generateValues | raw }}])
{% else %}
        $this->instance = $this->getMockBuilder({{ class.getName }}::class)
            ->disableOriginalConstructor()
{% endif %}
            ->getMockForTrait();
    }